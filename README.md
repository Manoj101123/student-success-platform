# Student Success Platform

React app: Advisor Dashboard and Student Portal.

## Run locally

```bash
npm install
npm start
```

## Build

```bash
npm run build
```

## Deploy (AWS Amplify)

Connect this repo to Amplify; build command `npm run build`, output directory `build`.
